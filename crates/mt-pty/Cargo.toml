[package]
name = "mt-pty"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true

[dependencies]
portable-pty.workspace = true
anyhow.workspace = true
parking_lot.workspace = true
# 纯逻辑共享库:WSL UNC 解析 / SSH 密码提示扫描 / ANSI 剥离。
# mt-core 仍留在 `src-tauri/` 下(还被 mt-sidecars 引用,见 docs/gpui-migration.md
# 第 7 节「未决项」),这里先用路径依赖过渡;等它整体挪进 `crates/` 后
# 换成 `mt-core.workspace = true` 即可,代码无需改动。
mt-core = { path = "../../src-tauri/mt-core" }
