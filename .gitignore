# Logs
logs
*.log

# mobile/ PWA 的 npm 依赖
node_modules

# release.yml 便携包组装目录
dist
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Brainstorm / temp files
.superpowers
.scratch/
.tmp/
tmp/

# Git worktrees
.worktrees

# Claude local settings
.claude/settings.local.json
# Claude 隔离 worktree（嵌套 git 仓库，git add -A 会当 gitlink 收进来）
.claude/worktrees/
# mini-term SSH MCP（本机相关配置，勿提交）
.mcp.json
.codex/

# 构建时下载并校验的官方 ConPTY 包缓存(staging 产物落在 target/ 下,已被忽略)
.conpty-cache/
# mini-term SSH skill（本机相关生成物，勿提交）
.claude/skills/mini-term-ssh/
.codex/skills/mini-term-ssh/

# 根工作区（Cargo.toml，members = crates/*）的构建产物
/target/

# GPUI 迁移主会话独立复核用的 target 目录
target-verify/
